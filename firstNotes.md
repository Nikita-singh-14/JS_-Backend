I satrt backend with javascript.

here i learn about mongoose, express.js, postman, and another tool that use to develop backend.

In this we deal only these three
1) Data
2) FIles
3) Third Party API


folder stucture
package.json , .env, Readme
src 
files => index(for database connection),  App(for config, cookies), constants(for enum, DB-name)

folder => 
DB(data store)
controllers
midlleware
models(define schema of data)
routes(for routing)
utils(resulble code)
